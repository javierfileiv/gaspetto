{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build C++ Program",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",                 // Generate debugging information
                "${workspaceFolder}/active_object_encapsulated.cpp",
		// "${workspaceFolder}/EventQueue.cpp", // Source file
                "-o",                 // Output flag
                "${workspaceFolder}/active_object_encapsulated"      // Output binary
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Task to build the C++ program for debugging."
        }
    ]
}